{
  "name": "parents-medical-helper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "nuxt typecheck",
    "check:policy": "node scripts/check-policy.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check": "npm run test && npm run typecheck && npm run check:policy && npm run generate"
  },
  "dependencies": {
    "html-to-image": "^1.11.13",
    "nuxt": "^3.15.4",
    "qrcode": "^1.5.4",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/qrcode": "^1.5.5",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "vue-tsc": "^2.2.0"
  }
}
